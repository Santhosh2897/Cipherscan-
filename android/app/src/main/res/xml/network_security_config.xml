<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Cleartext (HTTP) allowed everywhere — needed for local dev backends.
         Tighten this to specific domains before shipping to real users. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
