/**
 * CipherScan — ui package index
 *
 * UI components: Fragments, custom Views, and overlays.
 *
 * Files in this package:
 *   SecurityOverlayBottomSheet.kt — BottomSheetDialogFragment shown when a threat is
 *                                   detected. Renders the risk gauge, screenshot preview,
 *                                   redirect chain, threat reasons, and action buttons.
 *   RiskGaugeView.kt              — Custom View: semicircular arc gauge (0–100) with
 *                                   animated needle. Green ≤30, Amber ≤69, Red ≥70.
 *                                   (Implement this view — skeleton below.)
 */
package com.cipherscan.android.ui
