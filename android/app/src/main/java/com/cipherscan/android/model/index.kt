/**
 * CipherScan — model package index
 *
 * Data classes that mirror the backend JSON API responses.
 *
 * Files in this package:
 *   ScanResult.kt   — Response model for POST /api/analyze and GET /api/scans/:id.
 *                     Implements Parcelable for Fragment argument passing.
 *                     Also contains AnalyzeRequest (request body).
 */
package com.cipherscan.android.model
