/**
 * CipherScan — api package index
 *
 * Networking layer: Retrofit client + API interface definition.
 *
 * Files in this package:
 *   ApiService.kt     — Retrofit interface: POST /api/analyze
 *   RetrofitClient.kt — Singleton OkHttp + Retrofit instance, reads BuildConfig.CIPHERSCAN_SERVER_URL
 */
package com.cipherscan.android.api
