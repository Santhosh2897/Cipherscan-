module.exports = function handler(req, res) { try { var info = { ok: true, url: req.url, method: req.method, BACKEND_URL: process.env.BACKEND_URL ? 'SET' : 'NOT SET', APP_API_KEY: process.env.APP_API_KEY ? 'SET' : 'NOT SET', node: process.version }; res.setHeader('Content-Type','application/json'); res.status(200).end(JSON.stringify(info)); } catch(e) { res.status(500).end(JSON.stringify({error:e.message})); } }; 
