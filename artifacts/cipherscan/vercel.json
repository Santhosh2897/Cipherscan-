{
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @workspace/api-zod run build && pnpm --filter @workspace/db run build && pnpm --filter @workspace/api-client-react run build && cd artifacts/cipherscan && pnpm run build",
  "outputDirectory": "dist/public",
  "functions": {
    "api/proxy.ts": {
      "runtime": "@vercel/node@3"
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/proxy" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}